name: CI

on:
  workflow_dispatch:
  push:
    branches:
      - main
  pull_request:
    branches:
      - '*'

jobs:
  build-and-test:
    name: Build, Test, & Report Coverage
    runs-on: macos-latest

    steps:
      - name: Checkout source code
        uses: actions/checkout@v4

      - name: Set up Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: latest

      - name: Install dependencies
        run: swift package resolve

      - name: Build the package
        run: swift build

      - name: Run tests with code coverage
        run: swift test --enable-code-coverage

      - name: Gather code coverage
        run: xcrun llvm-cov export -format="lcov" .build/debug/SwiftSynchronizationPackageTests.xctest/Contents/MacOS/SwiftSynchronizationPackageTests -instr-profile .build/debug/codecov/default.profdata > coverage_report.lcov

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          files: coverage_report.lcov
          token: ${{ secrets.CODECOV_TOKEN }}
          slug: fetch-rewards/swift-synchronization
          verbose: true
          fail_ci_if_error: true